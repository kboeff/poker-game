from collections import Counter
from game_params import card_values, card_values, poker_scores

# create multiple little functions for each type of hand, like:
# def four_of_a_kind(hand):
#   return Boolean

# def score_hand(hand):
#     counts = Counter(map(lambda card: card.value, hand))
#     max_repeated = None
    # for count in counts:
    #     if count:
    #         return 1